# Portable runtime loader

Load `SKILL.md` as the entrypoint. Then read `AGENTS.md` for the detailed intraday RL
workflow, data boundary, output contract, and research-only limits. This adapter is
usable from Hermes and OpenClaw; it does not add credentials or runtime-specific state.
